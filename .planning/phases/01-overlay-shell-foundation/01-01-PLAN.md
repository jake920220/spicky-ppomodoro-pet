---
phase: 01-overlay-shell-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src/features/window-shell/desktop-window.ts
  - src/app/bootstrap.ts
autonomous: true
requirements:
  - DESK-01
user_setup: []
must_haves:
  truths:
    - Spiky launches as a transparent always-on-top overlay instead of a centered popup
    - The shell can be placed near the lower desktop area at startup
  artifacts:
    - src-tauri/tauri.conf.json configures the overlay shell correctly
    - src/features/window-shell/desktop-window.ts exposes an explicit startup placement path
  key_links:
    - bootstrap startup uses the window-shell adapter to apply lower-area placement
---

<objective>
Make the native shell feel like a desktop pet overlay from launch instead of a generic centered utility window.

Purpose: Establish the Tauri window defaults and startup placement behavior that everything else in Phase 1 depends on.
Output: Updated native shell config and a reusable startup placement path wired into app bootstrap.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-overlay-shell-foundation/01-CONTEXT.md
@.planning/phases/01-overlay-shell-foundation/01-RESEARCH.md
@AGENTS.md
@docs/product-rules.md
@docs/architecture-rules.md
@src-tauri/tauri.conf.json
@src-tauri/capabilities/default.json
@src/features/window-shell/desktop-window.ts
@src/app/bootstrap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align Tauri shell defaults to overlay startup behavior</name>
  <files>src-tauri/tauri.conf.json, src-tauri/capabilities/default.json</files>
  <read_first>src-tauri/tauri.conf.json, src-tauri/capabilities/default.json, .planning/phases/01-overlay-shell-foundation/01-CONTEXT.md</read_first>
  <action>Update `src-tauri/tauri.conf.json` so the main window remains transparent, undecorated, always-on-top, non-resizable, and sized for the compact dock plus pet stage, but no longer relies on `center: true` for startup behavior. Keep the main window label as `main`, preserve `visibleOnAllWorkspaces`, and keep the shell lightweight. Confirm `src-tauri/capabilities/default.json` still includes `core:window:allow-set-position` for startup positioning and do not widen the capability set beyond what the current shell already needs.</action>
  <verify>rg -n '\"label\": \"main\"|\"transparent\": true|\"alwaysOnTop\": true|\"decorations\": false|\"center\": false|allow-set-position' src-tauri/tauri.conf.json src-tauri/capabilities/default.json</verify>
  <acceptance_criteria>
    - `src-tauri/tauri.conf.json` contains `"transparent": true`
    - `src-tauri/tauri.conf.json` contains `"alwaysOnTop": true`
    - `src-tauri/tauri.conf.json` contains `"decorations": false`
    - `src-tauri/tauri.conf.json` contains `"center": false`
    - `src-tauri/capabilities/default.json` contains `"core:window:allow-set-position"`
  </acceptance_criteria>
  <done>Tauri shell defaults match an explicitly positioned overlay window and the capability file still grants only the needed positioning permission.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit lower-area startup placement via window-shell adapter</name>
  <files>src/features/window-shell/desktop-window.ts, src/app/bootstrap.ts</files>
  <read_first>src/features/window-shell/desktop-window.ts, src/app/bootstrap.ts, .planning/phases/01-overlay-shell-foundation/01-CONTEXT.md, .planning/phases/01-overlay-shell-foundation/01-RESEARCH.md</read_first>
  <action>Extend `DesktopWindowController` with a startup-oriented helper that resolves monitor work-area bounds and places the window near the lower desktop area using the existing `WINDOW_BOTTOM_MARGIN_PX` baseline. Compute X from the horizontal center of the available work area and Y from the lower safe baseline so the window feels grounded without deciding later walking behavior yet. Update `bootstrap()` to call this startup placement path before later interaction logic, and keep all Tauri-specific shell calls isolated inside the window-shell module.</action>
  <verify>pnpm build && cargo check --manifest-path src-tauri/Cargo.toml</verify>
  <acceptance_criteria>
    - `src/features/window-shell/desktop-window.ts` contains a startup placement helper in addition to `resolveWalkBounds`
    - `src/features/window-shell/desktop-window.ts` still contains `WINDOW_BOTTOM_MARGIN_PX`
    - `src/app/bootstrap.ts` contains a startup call that uses the window-shell adapter before normal runtime flow continues
    - `pnpm build` exits 0
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0
  </acceptance_criteria>
  <done>The shell has an explicit lower-area startup placement path and the app still passes frontend and Rust compile checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build`
- [ ] `cargo check --manifest-path src-tauri/Cargo.toml`
- [ ] Startup logic no longer depends on centered launch semantics
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Spiky's shell launches with overlay-first positioning and retains explicit native positioning support
  </success_criteria>

<output>
After completion, create `.planning/phases/01-overlay-shell-foundation/01-01-SUMMARY.md`
</output>
