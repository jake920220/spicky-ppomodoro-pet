---
phase: 01-overlay-shell-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/ui.ts
  - src/styles/app.css
autonomous: true
requirements:
  - DESK-02
  - DESK-03
user_setup: []
must_haves:
  truths:
    - The shell shows a compact top control dock instead of a large utility panel
    - No browser-style scrollbars appear in normal shell use
    - Dragging is limited to the dedicated handle
  artifacts:
    - src/app/ui.ts defines a top-only drag handle and compact dock structure
    - src/styles/app.css enforces overflow suppression at shell boundaries
  key_links:
    - The DOM structure and CSS selectors match the compact-dock + pet-stage layout
---

<objective>
Refine the shell layout so the top controls stay compact, the drag behavior is unambiguous, and the overlay never leaks browser-like scroll UI.

Purpose: Deliver the visible shell structure and density that make Spiky feel ambient instead of like a generic desktop app.
Output: Updated markup and CSS for the compact dock, drag handle, and no-scroll shell layout.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-overlay-shell-foundation/01-CONTEXT.md
@.planning/phases/01-overlay-shell-foundation/01-RESEARCH.md
@AGENTS.md
@src/app/ui.ts
@src/styles/app.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the compact top-dock structure in the DOM template</name>
  <files>src/app/ui.ts</files>
  <read_first>src/app/ui.ts, .planning/phases/01-overlay-shell-foundation/01-CONTEXT.md</read_first>
  <action>Update `src/app/ui.ts` so the shell markup keeps the control surface limited to a top `control-dock` with a single `drag-handle`, the minute input, the timer readout/status block, and the core buttons only. Keep the pet display in a separate `pet-stage` below the dock. Do not add extra helper sections, secondary settings panels, or drag behavior outside `data-tauri-drag-region` on the top handle.</action>
  <verify>rg -n 'control-dock|drag-handle|data-tauri-drag-region|pet-stage|timer-actions' src/app/ui.ts</verify>
  <acceptance_criteria>
    - `src/app/ui.ts` contains `<section class="control-dock">`
    - `src/app/ui.ts` contains `data-tauri-drag-region`
    - `src/app/ui.ts` contains `<section class="pet-stage"`
    - `src/app/ui.ts` does not introduce an additional settings/detail panel section beyond the control dock and pet stage
  </acceptance_criteria>
  <done>The DOM template clearly expresses a minimal top dock and a separate lower pet stage.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce no-scroll shell layout and compact dock density in CSS</name>
  <files>src/styles/app.css</files>
  <read_first>src/styles/app.css, src/app/ui.ts, .planning/phases/01-overlay-shell-foundation/01-CONTEXT.md, .planning/phases/01-overlay-shell-foundation/01-RESEARCH.md</read_first>
  <action>Update `src/styles/app.css` so `html`, `body`, and `#app` all keep `overflow: hidden`, the shell uses a top-dock plus lower-stage grid, and the dock remains visually compact. Preserve a dedicated drag-handle strip, keep the pet stage aligned toward the lower part of the shell, and make the pet stage visually more prominent than the dock. Keep the implementation lightweight: pure CSS, no JS-driven layout workaround.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - `src/styles/app.css` contains `overflow: hidden` for `html`, `body`, and `#app`
    - `src/styles/app.css` contains `.app-shell` with `grid-template-rows: auto minmax(0, 1fr)`
    - `src/styles/app.css` contains `.drag-handle`
    - `src/styles/app.css` contains `.pet-stage`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>The overlay shell is compact, scroll-free, and visually structured around a top dock plus grounded pet stage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build`
- [ ] DOM structure still matches CSS selectors
- [ ] Scrollbars do not reappear through layout overflow regressions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shell markup and CSS together implement a minimal top dock with no visible scrollbars
  </success_criteria>

<output>
After completion, create `.planning/phases/01-overlay-shell-foundation/01-02-SUMMARY.md`
</output>
