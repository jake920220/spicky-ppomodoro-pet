---
phase: 01-overlay-shell-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/app/bootstrap.ts
  - src/app/ui.ts
  - src/styles/app.css
  - src/shared/assets/manifest.ts
  - src/features/spiky-state/spiky-state.ts
autonomous: true
requirements:
  - PET-01
user_setup: []
must_haves:
  truths:
    - Spiky appears in a stable default idle state using image_1 as soon as the shell is ready
    - The idle shell already reads as a grounded desktop pet instead of a plain utility panel
  artifacts:
    - bootstrap renders the idle pet state and image fallback path safely
    - UI and CSS reinforce the idle-stage presentation
  key_links:
    - default visual state maps to the correct asset and shell presentation without waiting for timer interaction
---

<objective>
Finish Phase 1 by making the default shell state visibly and structurally ready for the pet, not just for the controls.

Purpose: Ensure the app opens into a stable idle Spiky presence that already feels grounded and companion-like before Phase 2 adds walking/click personality.
Output: Baseline idle rendering, shell-stage polish, and safe startup/fallback wiring around the default asset.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-overlay-shell-foundation/01-CONTEXT.md
@.planning/phases/01-overlay-shell-foundation/01-RESEARCH.md
@AGENTS.md
@src/app/bootstrap.ts
@src/app/ui.ts
@src/styles/app.css
@src/shared/assets/manifest.ts
@src/features/spiky-state/spiky-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make default idle rendering explicit and safe on startup</name>
  <files>src/app/bootstrap.ts, src/app/ui.ts, src/shared/assets/manifest.ts, src/features/spiky-state/spiky-state.ts</files>
  <read_first>src/app/bootstrap.ts, src/app/ui.ts, src/shared/assets/manifest.ts, src/features/spiky-state/spiky-state.ts, .planning/phases/01-overlay-shell-foundation/01-CONTEXT.md</read_first>
  <action>Update startup rendering so the default visual state is explicitly treated as the idle shell state for Phase 1. Keep `image_1` as the canonical idle asset in `src/shared/assets/manifest.ts`, preserve the existing default visual state in `SpikyStateController`, and ensure `bootstrap()` renders the idle shell immediately while still maintaining `showImageFallback` / `hideImageFallback` behavior for missing asset failures.</action>
  <verify>rg -n 'default: \"/assets/images/image_1.png\"|visual: \"default\"|showImageFallback|hideImageFallback' src/shared/assets/manifest.ts src/features/spiky-state/spiky-state.ts src/app/bootstrap.ts</verify>
  <acceptance_criteria>
    - `src/shared/assets/manifest.ts` contains `default: "/assets/images/image_1.png"`
    - `src/features/spiky-state/spiky-state.ts` contains `visual: "default"`
    - `src/app/bootstrap.ts` contains both `showImageFallback` and `hideImageFallback`
    - `src/app/bootstrap.ts` performs an initial `render()` before later interactions occur
  </acceptance_criteria>
  <done>The shell reliably opens in the default idle state and retains safe asset fallback handling.</done>
</task>

<task type="auto">
  <name>Task 2: Reinforce grounded idle-stage presentation in the shell visuals</name>
  <files>src/app/ui.ts, src/styles/app.css</files>
  <read_first>src/app/ui.ts, src/styles/app.css, .planning/phases/01-overlay-shell-foundation/01-CONTEXT.md, .planning/phases/01-overlay-shell-foundation/01-RESEARCH.md</read_first>
  <action>Refine the existing `pet-stage`, `pet-button`, `pet-stage__glow`, and related shell selectors so the idle shell already looks grounded and pet-first. Keep the implementation lightweight and desktop-pet oriented: visible stage/shadow cues, visual emphasis on the pet, and no extra copy-heavy presentation in the dock. Preserve the top-dock minimalism decided in Phase 1 context.</action>
  <verify>pnpm build && cargo check --manifest-path src-tauri/Cargo.toml</verify>
  <acceptance_criteria>
    - `src/app/ui.ts` still contains `pet-stage` and `pet-button`
    - `src/styles/app.css` contains selectors for `.pet-stage`, `.pet-stage::after`, and `.pet-button`
    - `src/styles/app.css` contains a visual grounding selector such as `.pet-stage__glow` or equivalent stage/shadow treatment
    - `pnpm build` exits 0
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0
  </acceptance_criteria>
  <done>The default overlay state visibly reads as a grounded desktop pet shell and the app still passes compile checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build`
- [ ] `cargo check --manifest-path src-tauri/Cargo.toml`
- [ ] Default shell opens into an idle pet-first presentation using `image_1`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 finishes with a stable idle Spiky shell that feels grounded and companion-like
  </success_criteria>

<output>
After completion, create `.planning/phases/01-overlay-shell-foundation/01-03-SUMMARY.md`
</output>
