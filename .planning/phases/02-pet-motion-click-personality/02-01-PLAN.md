---
phase: 02-pet-motion-click-personality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/desktop-walker/desktop-walker.ts
  - src/features/spiky-state/spiky-state.ts
  - src/features/audio/audio-player.ts
  - src/app/bootstrap.ts
  - src/app/ui.ts
autonomous: true
requirements:
  - PET-02
  - PET-03
  - AUD-01
user_setup: []
must_haves:
  truths:
    - Spiky walks less often and with more obviously random cadence than Phase 1
    - Clicking Spiky no longer changes its movement direction just to show a reaction
    - image_2 remains visible until click audio playback actually ends
  artifacts:
    - desktop-walker supports quieter cadence windows without fixed-feeling timing
    - click-state logic no longer relies on a fixed primary timeout
    - audio playback can notify the app when the click cue completes
  key_links:
    - bootstrap coordinates click audio completion with clicked visual-state restoration
---

<objective>
Refine the pet's normal walking and click-response behavior so Spiky feels calmer, less periodic, and more tightly synced to the click audio.

Purpose: Deliver the core Phase 2 personality changes for movement cadence and click response without breaking the existing shell.
Output: Updated walker cadence, direction-preserving click behavior, and audio-end-driven clicked-state restoration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pet-motion-click-personality/02-CONTEXT.md
@.planning/phases/02-pet-motion-click-personality/02-RESEARCH.md
@.planning/phases/01-overlay-shell-foundation/01-03-SUMMARY.md
@AGENTS.md
@src/features/desktop-walker/desktop-walker.ts
@src/features/spiky-state/spiky-state.ts
@src/features/audio/audio-player.ts
@src/app/bootstrap.ts
@src/app/ui.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce walking frequency and make cadence feel less periodic</name>
  <files>src/features/desktop-walker/desktop-walker.ts, src/app/ui.ts</files>
  <read_first>src/features/desktop-walker/desktop-walker.ts, src/app/ui.ts, .planning/phases/02-pet-motion-click-personality/02-CONTEXT.md, .planning/phases/02-pet-motion-click-personality/02-RESEARCH.md</read_first>
  <action>Update `src/features/desktop-walker/desktop-walker.ts` so normal movement feels more infrequent than the current implementation. Increase the rest windows and, if needed, randomize walking-duration behavior as well so the pet does not feel locked into a predictable short-walk/short-rest loop. Keep timer-running movement frequency at less than half of the already-reduced baseline, and keep the walking caption in `src/app/ui.ts` as `데스크톱 산책 중.` even while the timer is running.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - `src/features/desktop-walker/desktop-walker.ts` contains widened random cadence constants for normal rest windows
    - `src/features/desktop-walker/desktop-walker.ts` contains widened timer-running cadence constants that are clearly more sparse than normal movement
    - `src/app/ui.ts` contains `return "데스크톱 산책 중."` for walking caption logic
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>The walker cadence is less frequent and less periodic, and the walking caption remains unified during timer-running movement.</done>
</task>

<task type="auto">
  <name>Task 2: Make clicked state follow audio playback end without forcing direction changes</name>
  <files>src/features/spiky-state/spiky-state.ts, src/features/audio/audio-player.ts, src/app/bootstrap.ts, src/features/desktop-walker/desktop-walker.ts</files>
  <read_first>src/features/spiky-state/spiky-state.ts, src/features/audio/audio-player.ts, src/app/bootstrap.ts, src/features/desktop-walker/desktop-walker.ts, .planning/phases/02-pet-motion-click-personality/02-CONTEXT.md, .planning/phases/02-pet-motion-click-personality/02-RESEARCH.md</read_first>
  <action>Refactor click behavior so the click visual state is restored primarily when the click audio actually finishes. Extend `src/features/audio/audio-player.ts` with a cue-specific completion callback or equivalent ended-event mechanism, and update `src/features/spiky-state/spiky-state.ts` so clicked-state restoration no longer depends on a fixed primary timeout. Keep a fail-safe timeout only as a fallback path. In `src/app/bootstrap.ts`, wire the click flow so Spiky keeps its current direction/path when clicked; the click should trigger image/audio reaction but should not reverse movement direction anymore.</action>
  <verify>pnpm build && cargo check --manifest-path src-tauri/Cargo.toml</verify>
  <acceptance_criteria>
    - `src/features/audio/audio-player.ts` contains a playback-end handling path for the `click` cue
    - `src/features/spiky-state/spiky-state.ts` no longer uses the fixed timeout as the primary clicked-state lifetime mechanism
    - `src/app/bootstrap.ts` no longer flips walker direction as part of normal click handling
    - `pnpm build` exits 0
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0
  </acceptance_criteria>
  <done>Click reactions preserve the current movement path while keeping image_2 alive until click audio completion, with a safe fallback path if playback events fail.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build`
- [ ] `cargo check --manifest-path src-tauri/Cargo.toml`
- [ ] Click direction is no longer forcibly changed by the click handler
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 movement cadence and click-response core behavior feel less periodic and more audio-synced
  </success_criteria>

<output>
After completion, create `.planning/phases/02-pet-motion-click-personality/02-01-SUMMARY.md`
</output>
