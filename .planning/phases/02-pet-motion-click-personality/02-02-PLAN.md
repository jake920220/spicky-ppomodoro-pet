---
phase: 02-pet-motion-click-personality
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/features/desktop-walker/desktop-walker.ts
  - src/features/audio/audio-player.ts
  - src/app/bootstrap.ts
  - src/app/ui.ts
  - src/styles/app.css
  - src/features/spiky-state/spiky-state.ts
autonomous: true
requirements:
  - PET-02
user_setup: []
must_haves:
  truths:
    - While the timer is finished, Spiky stops lateral walking and jumps in place instead
    - The finished alert sound repeats until dismissal with an intentional gap between repeats
    - Dismissing the alert stops the repeat audio, stops the jump animation, and returns the pet to default idle
  artifacts:
    - bootstrap coordinates timer-finished repeat playback and dismiss cleanup
    - walker supports a non-walking alert behavior mode
    - UI/CSS expose a finished-state jump animation hook
  key_links:
    - timer finished state in bootstrap drives both repeated audio scheduling and in-place jump presentation
---

<objective>
Turn the timer-finished state into a distinct alert mode with repeated audio, in-place jump animation, and clean dismissal behavior.

Purpose: Complete the personality layer for focus/alert behavior so Spiky feels intentionally reactive rather than just reusing normal movement during completion.
Output: Timer-finished repeat playback loop, jump-in-place alert mode, and dismiss-to-idle restoration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pet-motion-click-personality/02-CONTEXT.md
@.planning/phases/02-pet-motion-click-personality/02-RESEARCH.md
@.planning/phases/02-pet-motion-click-personality/02-01-PLAN.md
@AGENTS.md
@src/features/desktop-walker/desktop-walker.ts
@src/features/audio/audio-player.ts
@src/app/bootstrap.ts
@src/app/ui.ts
@src/styles/app.css
@src/features/spiky-state/spiky-state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timer-finished repeat alert playback with clean dismiss behavior</name>
  <files>src/features/audio/audio-player.ts, src/app/bootstrap.ts, src/features/spiky-state/spiky-state.ts</files>
  <read_first>src/features/audio/audio-player.ts, src/app/bootstrap.ts, src/features/spiky-state/spiky-state.ts, .planning/phases/02-pet-motion-click-personality/02-CONTEXT.md, .planning/phases/02-pet-motion-click-personality/02-RESEARCH.md</read_first>
  <action>Extend the timer-finished alert flow so the `timerFinished` cue repeats until dismissal, but never as an immediate harsh loop. Schedule the next playback only after the prior playback ends and then wait about 2 seconds before replaying. In `src/app/bootstrap.ts`, make the dismissal path stop the repeat scheduler, stop any currently playing finished cue, stop the finished-state movement mode, and return Spiky to the default `image_1` idle state.</action>
  <verify>pnpm build && cargo check --manifest-path src-tauri/Cargo.toml</verify>
  <acceptance_criteria>
    - `src/features/audio/audio-player.ts` contains repeat scheduling support or equivalent playback lifecycle hooks for `timerFinished`
    - `src/app/bootstrap.ts` contains explicit cleanup for timer-finished repeat playback on dismiss/reset
    - `src/features/spiky-state/spiky-state.ts` still restores default idle state on dismiss
    - `pnpm build` exits 0
    - `cargo check --manifest-path src-tauri/Cargo.toml` exits 0
  </acceptance_criteria>
  <done>Timer-finished audio repeats with a visible cooldown gap and stops immediately when the user dismisses the alert.</done>
</task>

<task type="auto">
  <name>Task 2: Add in-place jump alert mode for timer-finished state</name>
  <files>src/features/desktop-walker/desktop-walker.ts, src/app/ui.ts, src/styles/app.css, src/app/bootstrap.ts</files>
  <read_first>src/features/desktop-walker/desktop-walker.ts, src/app/ui.ts, src/styles/app.css, src/app/bootstrap.ts, .planning/phases/02-pet-motion-click-personality/02-CONTEXT.md, .planning/phases/02-pet-motion-click-personality/02-RESEARCH.md</read_first>
  <action>Introduce a finished-state movement mode where Spiky stops lateral walking and instead uses an in-place jump animation. Keep the jump localized to the character rather than moving the whole window side to side. Use the existing render/data-attribute path so `timer_finished` drives a dedicated alert animation in CSS. Ensure the dismiss path removes that alert mode and returns the shell to normal idle/walking behavior.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - `src/features/desktop-walker/desktop-walker.ts` contains a finished/alert movement mode that disables lateral walking
    - `src/styles/app.css` contains a dedicated jump-like animation used for the finished state
    - `src/app/ui.ts` still maps the finished visual state cleanly while preserving normal captions elsewhere
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Timer-finished state becomes a distinct in-place jump alert mode and dismissing the alert restores normal pet behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build`
- [ ] `cargo check --manifest-path src-tauri/Cargo.toml`
- [ ] Timer-finished behavior no longer uses normal lateral walking
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Timer-finished behavior clearly feels like a special alert mode with repeat audio and in-place motion
  </success_criteria>

<output>
After completion, create `.planning/phases/02-pet-motion-click-personality/02-02-SUMMARY.md`
</output>
